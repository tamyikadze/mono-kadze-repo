REACT_EDITOR=webstorm

# Database Configuration
DB_HOST=localhost
DB_USERNAME=postgres
POSTGRES_PASSWORD=password
DB_PASSWORD=password
DB_NAME=example

# JWT Configuration (if using custom JWT)
JWT_TOKEN=token
JWT_ISSUER=example

# Better Auth Configuration
AUTH_SECRET="tGt6ghIu5bdamZzPFgFo69y7yC9rAoNTr7bQJIF35iw=" # Generate with: openssl rand -base64 32
AUTH_TRUST_HOST=true

# API URLs
# For API server (Fastify backend)
SERVER_URL=http://localhost:4000
BETTER_AUTH_URL=http://localhost:4000
CLIENT_ORIGIN=http://localhost:3000
API_URL=http://localhost:4000

# For web client (Next.js) - must use NEXT_PUBLIC_ prefix for browser access
# Points to Next.js app (not Fastify) - Next.js rewrites proxy /api/auth/* to Fastify
NEXT_PUBLIC_API_URL=http://localhost:3000
